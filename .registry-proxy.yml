# 用于安装本工程的仓库代理配置，须与 package.json 置于同一目录
registries:
  "https://registry.npmjs.org": { }

# 代理服务器https配置 可空
#https:
#  key: ~/.ssl/key.pem
#  cert: ~/.ssl/cert.pem

# 代理服务器basePath字符串 可空
#basePath: "npm-proxy"