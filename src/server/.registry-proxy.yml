# 用于registry-proxy server测试的仓库代理配置，本文件应当默认放在registry-proxy server工作路径内
registries:
  "https://registry.npmjs.org": { }

# 代理服务器https配置 可空
#https:
#  key: ~/.ssl/key.pem
#  cert: ~/.ssl/cert.pem

# 代理服务器basePath字符串 可空
#basePath: "npm-proxy"